let card = require("../src/main")
beforeEach,
function () {

	const dom = new jsDOM
	describe('memory game tests', function () {

		it('must have 12 cards', function () {
			expect(game.card.length).toEqual(12)
		});

	})
	it("should be able to add addEventListener to all html elements", function () {
		let shuffle = require("../src/main")

		expect(shuffle).toBeDefined();

	})

	it("should be able to add addEventListener to all html elements", function () {
		let resetBoard = require("../src/main")

		expect(resetBoard).toBeDefined();
	});
};